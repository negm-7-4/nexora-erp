{
  "name": "nile-app-server",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "MongoDB-backed API for the Nile ERP app (auth + per-tenant data sync).",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.9.5"
  },
  "devDependencies": {
    "mongodb-memory-server": "^11.1.0"
  }
}
